

A) get the tree on screen again
B) purge the tile abstraction from the code - won't be needed or missed
C) implement the better timer as a utility module

X) clean comments to utilize word wrap instead of new lines